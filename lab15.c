#include <stdio.h> 
#include <string.h> 

int main() {
    // Объявляем буфер для ввода (максимум 100 символов)
    char input[100]; 
    
    // Выводим приглашение для пользователя
    printf("Введите число: ");
    // Считываем ввод как строку (чтобы сохранить все нули)
    scanf("%s", input);
    
    // Определяем длину введенной строки
    int length = strlen(input);
    // Флаг для проверки, все ли символы - нули (изначально true)
    int all_zeros = 1;
    
    // Проверяем каждый символ строки
    for(int i = 0; i < length; i++) {
        // Если нашли символ, не равный '0'
        if(input[i] != '0') {
            all_zeros = 0; // Устанавливаем флаг в false
            break;        // Прерываем проверку
        }
    }
    
    // Если все символы - нули
    if(all_zeros) {
        // Выводим заголовок для прямого порядка
        printf("Цифры в прямом порядке: ");
        // Печатаем все нули в прямом порядке
        for(int i = 0; i < length; i++) {
            printf("0 "); // Выводим ноль с пробелом
        }
        
        // Выводим заголовок для обратного порядка
        printf("\nЦифры в обратном порядке: ");
        // Печатаем все нули в обратном порядке
        for(int i = 0; i < length; i++) {
            printf("0 "); // Выводим ноль с пробелом
        }
    }
    // Если есть другие цифры кроме нулей
    else {
        // Выводим заголовок для прямого порядка
        printf("Цифры в прямом порядке: ");
        // цифры в прямом порядке
        for(int i = 0; i < length; i++) {
            // Проверяем, что символ является цифрой
            if(input[i] >= '0' && input[i] <= '9') {
                printf("%c ", input[i]); // Выводим цифру с пробелом
            }
        }
        
        // Выводим заголовок для обратного порядка
        printf("\nЦифры в обратном порядке: ");
        // цифры в обратном порядке
        for(int i = length-1; i >= 0; i--) {
            // Проверяем, что символ является цифрой
            if(input[i] >= '0' && input[i] <= '9') {
                printf("%c ", input[i]); 
            }
        }
    }
    
    
    printf("\n");
    return 0; 
}
